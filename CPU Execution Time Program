
#include <stdio.h>
#include <time.h>

int main()
{
    clock_t start, end;
    double cpu_time;

    start = clock();

    printf("\nThe numbers from 1 to 100 are: ");
    for(int i=0; i<=100; i++)
    {
        printf("%d\t", i);
    }

    end = clock();

    cpu_time = (double)(end - start) / CLOCKS_PER_SEC;
    printf("\n\n%f\n", cpu_time);
    return 0;
}
